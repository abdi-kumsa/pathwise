# /routers

FastAPI route handlers. One file per domain:

- `market.py` — GET endpoints for hiring analytics, skill gap, regional data
- `enrollment.py` — GET/POST endpoints for enrollment forecasts and placement prediction
- `recommendations.py` — POST endpoints for skill recommendations and escape routes
