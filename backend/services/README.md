# /services

Business logic and ML inference wrappers:

- `ml_service.py` — loads trained models on startup, exposes predict_placement(), forecast_enrollment(), etc.
- `recommendation_service.py` — enrollment recommendation logic, skill gap engine, escape route mapper
