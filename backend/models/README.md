# /models

SQLAlchemy ORM table definitions. One file per database table:

- `student.py` — Student table
- `enrollment.py` — Enrollment table
- `job_posting.py` — JobPosting table
- `placement.py` — Placement table
